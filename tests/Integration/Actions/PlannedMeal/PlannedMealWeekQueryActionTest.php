<?php

namespace Tests\Integration\Actions\PlannedMeal;

describe('PlannedMealWeekQueryAction', function () {
    test('returns only meals belonging to the current workspace', function () {
        /** @var \Tests\TestCase $this */
    });

    test('returns meals for a specific week range', function () {
        /** @var \Tests\TestCase $this */
    });

    test('returns empty collection when no meals exist for the week', function () {
        /** @var \Tests\TestCase $this */
    });
});
